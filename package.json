{
  "name": "online-compiler",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.js",
  "scripts": {
    "build-client": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "client": "npm run install-client && npm run build-client",
    "start": "node src/index.js",
    "dev": "npm run client && nodemon --inspect -r dotenv/config ./src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.12.13"
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "prettier": "^2.3.1"
  },
  "engines": {
    "node": "14.16.1",
    "npm": "7.16.0"
  }
}